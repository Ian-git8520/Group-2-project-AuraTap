'use client'

import * as React from 'react'

import { cn } from '@/lib/utils'

function Table({ className, ...props }.ComponentProps) {
  return (
    
      
    
  )
}

function TableHeader({ className, ...props }.ComponentProps) {
  return (
    
  )
}

function TableBody({ className, ...props }.ComponentProps) {
  return (
    
  )
}

function TableFooter({ className, ...props }.ComponentProps) {
  return (
    tr]-b-0',
        className,
      )}
      {...props}
    />
  )
}

function TableRow({ className, ...props }.ComponentProps) {
  return (
    
  )
}

function TableHead({ className, ...props }.ComponentProps) {
  return (
    [role=checkbox]]-y-[2px]',
        className,
      )}
      {...props}
    />
  )
}

function TableCell({ className, ...props }.ComponentProps) {
  return (
    [role=checkbox]]-y-[2px]',
        className,
      )}
      {...props}
    />
  )
}

function TableCaption({
  className,
  ...props
}.ComponentProps) {
  return (
    
  )
}

export {
  Table,
  TableHeader,
  TableBody,
  TableFooter,
  TableHead,
  TableRow,
  TableCell,
  TableCaption,
}
